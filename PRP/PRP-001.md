PRP-001: INVENTAIRE INTELLIGENT - REDESIGN COMPLET
Inspiration: Sorted³, Things 3, Notion🎯 OBJECTIF
Transformer l'inventaire en hub intelligent où trouver un produit prend < 2 secondes, avec une UX fluide inspirée des meilleures apps de productivité.📱 BENCHMARKS ANALYSÉS

Sorted³ : Organisation spatiale et temporelle
Things 3 : Simplicité et élégance
Paprika : Gestion alimentaire
Bring! : Liste collaborative
🎨 NOUVEAU DESIGN INVENTAIREtypescript// Structure de la page Inventaire repensée
interface InventoryPageRedesign {
  // 1. HEADER INTELLIGENT (fixe)
  header: {
    searchBar: {
      type: 'instant-search';
      placeholder: 'Tomate, Pâtes, Produits frais...';
      features: [
        'auto-complete',
        'voice-input',
        'barcode-scanner-icon',
        'recent-searches'
      ];
    };
    quickFilters: [
      { icon: '🔴', label: 'Expire bientôt', count: 3 },
      { icon: '❄️', label: 'Frigo', active: false },
      { icon: '🥫', label: 'Placard', active: false },
      { icon: '🧊', label: 'Congélateur', active: false }
    ];
    statsBar: {
      totalItems: 47,
      expiringThisWeek: 3,
      lowStock: 5
    };
  };

  // 2. ZONES VISUELLES (comme un frigo virtuel)
  mainView: {
    layout: 'grid' | 'list' | 'zones';
    zones: [
      {
        name: 'Frigo',
        icon: '❄️',
        color: 'blue',
        sections: [
          { name: 'Produits frais', items: [] },
          { name: 'Produits laitiers', items: [] },
          { name: 'Boissons', items: [] }
        ]
      },
      {
        name: 'Congélateur',
        icon: '🧊',
        color: 'cyan',
        sections: [
          { name: 'Viandes', items: [] },
          { name: 'Légumes surgelés', items: [] }
        ]
      },
      {
        name: 'Placard',
        icon: '🥫',
        color: 'amber',
        sections: [
          { name: 'Conserves', items: [] },
          { name: 'Pâtes & Riz', items: [] },
          { name: 'Épices', items: [] }
        ]
      }
    ];
  };

  // 3. FLOATING ACTION BUTTON (Material Design 3)
  fab: {
    primary: {
      icon: '+',
      action: 'quick-add',
      longPress: 'voice-add'
    };
    expandedOptions: [
      { icon: '📷', label: 'Scanner', action: 'camera-scan' },
      { icon: '🎤', label: 'Dicter', action: 'voice-input' },
      { icon: '📝', label: 'Manuel', action: 'manual-add' },
      { icon: '🛒', label: 'Ticket', action: 'receipt-scan' }
    ];
  };
}🎯 COMPOSANT: CARTE PRODUIT INTELLIGENTEtsx// ProductCard avec toutes les interactions
const SmartProductCard = ({ product }) => {
  return (
    <motion.div
      className="relative bg-white rounded-2xl p-4 shadow-sm"
      whileHover={{ scale: 1.02 }}
      whileTap={{ scale: 0.98 }}
    >
      {/* Badge Expiration Dynamique */}
      {product.daysUntilExpiry <= 3 && (
        <div className="absolute -top-2 -right-2">
          <motion.div
            animate={{ scale: [1, 1.2, 1] }}
            transition={{ repeat: Infinity, duration: 2 }}
            className={cn(
              "w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold",
              product.daysUntilExpiry <= 1 ? "bg-red-500" : "bg-orange-500"
            )}
          >
            {product.daysUntilExpiry}j
          </motion.div>
        </div>
      )}

      {/* Image avec placeholder intelligent */}
      <div className="aspect-square rounded-lg bg-gray-100 mb-3 overflow-hidden">
        {product.image ? (
          <img src={product.image} className="w-full h-full object-cover" />
        ) : (
          <div className="w-full h-full flex items-center justify-center text-4xl">
            {getEmojiForCategory(product.category)}
          </div>
        )}
      </div>

      {/* Informations */}
      <h3 className="font-medium text-gray-900 mb-1">{product.name}</h3>
      
      {/* Quantité Interactive */}
      <div className="flex items-center justify-between mb-2">
        <QuantitySelector
          value={product.quantity}
          unit={product.unit}
          onChange={(val) => updateQuantity(product.id, val)}
          quick={true} // Boutons +/- rapides
        />
      </div>

      {/* Actions Swipe sur Mobile */}
      <SwipeableActions
        onSwipeLeft={() => moveToShoppingList(product)}
        onSwipeRight={() => consumeProduct(product)}
        leftAction={{ icon: '🛒', color: 'blue', label: 'Courses' }}
        rightAction={{ icon: '✅', color: 'green', label: 'Consommé' }}
      />

      {/* Quick Actions Desktop */}
      <div className="flex gap-2 mt-3 opacity-0 group-hover:opacity-100 transition-opacity">
        <IconButton icon="📝" onClick={() => editProduct(product)} />
        <IconButton icon="🍳" onClick={() => findRecipes(product)} />
        <IconButton icon="🔄" onClick={() => substituteProduct(product)} />
      </div>
    </motion.div>
  );
};🔍 RECHERCHE INSTANTANÉE INTELLIGENTEtsxconst IntelligentSearch = () => {
  const [query, setQuery] = useState('');
  const [mode, setMode] = useState<'text' | 'voice' | 'image'>('text');
  
  return (
    <div className="relative">
      {/* Barre de recherche principale */}
      <div className="flex items-center bg-gray-50 rounded-2xl p-3">
        <Search className="w-5 h-5 text-gray-400 mr-3" />
        
        <input
          type="text"
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          placeholder="Rechercher par nom, catégorie, date..."
          className="flex-1 bg-transparent outline-none"
        />
        
        {/* Boutons mode */}
        <div className="flex gap-2 ml-2">
          <IconButton
            icon={<Mic />}
            active={mode === 'voice'}
            onClick={() => setMode('voice')}
          />
          <IconButton
            icon={<Camera />}
            active={mode === 'image'}
            onClick={() => setMode('image')}
          />
        </div>
      </div>

      {/* Suggestions en temps réel */}
      <AnimatePresence>
        {query && (
          <motion.div
            initial={{ opacity: 0, y: -10 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0, y: -10 }}
            className="absolute top-full mt-2 w-full bg-white rounded-xl shadow-lg p-2 z-50"
          >
            {/* Résultats groupés */}
            <SearchResults
              groups={[
                {
                  title: 'Produits',
                  items: searchResults.products,
                  icon: '📦'
                },
                {
                  title: 'Catégories',
                  items: searchResults.categories,
                  icon: '📁'
                },
                {
                  title: 'Recettes possibles',
                  items: searchResults.recipes,
                  icon: '🍳'
                }
              ]}
            />
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
};