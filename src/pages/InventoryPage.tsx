import Layout from "@/components/Layout";
import Inventory from "./Inventory";

const InventoryPage = () => {
  return (
    <Layout>
      <Inventory />
    </Layout>
  );
};

export default InventoryPage;